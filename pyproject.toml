[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gif-maker"
version = "1.0.9"
description = "Professional Animated GIF Creation Tool"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyautogui>=0.9.54",
    "Pillow>=12.3.0,<13",
    "pygetwindow>=0.0.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
build = [
    "pyinstaller>=6.0",
]

[project.scripts]
gif-maker = "gif_maker:main"

[tool.setuptools.packages.find]
include = ["gif_maker*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
